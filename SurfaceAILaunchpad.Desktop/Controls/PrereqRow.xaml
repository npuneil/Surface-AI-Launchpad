<UserControl
    x:Class="SurfaceAILaunchpad.Desktop.Controls.PrereqRow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="#13131f" CornerRadius="8" Padding="14">
        <Grid ColumnSpacing="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="28"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <FontIcon Grid.Column="0" x:Name="StatusIcon"
                      Glyph="&#xE73E;" FontSize="20" Foreground="#666"
                      VerticalAlignment="Top" Margin="0,2,0,0"/>

            <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock x:Name="NameText" FontSize="15" FontWeight="SemiBold" Foreground="#e8e8f0"/>
                <TextBlock x:Name="DescText" FontSize="12" Foreground="#9b9bbf" TextWrapping="Wrap"/>
                <TextBlock x:Name="DetailText" FontSize="11" Foreground="#6f7090" TextWrapping="Wrap"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="6"
                        VerticalAlignment="Center">
                <ProgressRing x:Name="BusyRing" IsActive="False" Width="18" Height="18"/>
                <Button x:Name="ActionButton" Content="Install" Click="OnAction"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
